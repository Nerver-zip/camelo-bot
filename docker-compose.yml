name: camelo-bot

services:
  camelo-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env
    environment:
      NODE_ENV: production
      CHROME_PATH: /usr/bin/chromium
    restart: unless-stopped
    volumes:
      - camelo-dump:/app/src/local/dump
      - camelo-charts:/app/src/local/charts
      - camelo-history:/app/src/automessages/mirrors

volumes:
  camelo-dump:
    name: camelo-dump
  camelo-charts:
    name: camelo-charts
  camelo-history:
    name: camelo-history
